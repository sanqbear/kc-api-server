PORT=8080
APP_ENV=local

DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=knowledgecenter
DB_USERNAME=kc_svc
DB_PASSWORD=password!1
DB_SCHEMA=kc_generic
DB_ADMIN_SCHEMA=kc_admin

# Encryption key for AES-256-GCM encryption (user contact information)
ENCRYPTION_KEY=your-32-byte-encryption-key-here

# JWT secret for signing access tokens
JWT_SECRET=your-jwt-secret-key-change-in-production

# EWS (Exchange Web Services) Plugin Configuration (Optional)
# Set EWS_SERVER_URL to enable the EWS plugin
# EWS_SERVER_URL=https://mail.example.com/EWS/Exchange.asmx
# EWS_IMPERSONATION_USERNAME=service-account@example.com
# EWS_IMPERSONATION_PASSWORD=your-service-account-password
# EWS_DOMAIN=example.com
# EWS_TIMEOUT=30s
# EWS_MAX_RETRIES=3
# EWS_SKIP_TLS_VERIFY=false